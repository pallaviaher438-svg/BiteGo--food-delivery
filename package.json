{
  "name": "bitego-food-delivery",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "frontend": "cd frontend && bun run dev",
    "backend": "cd backend && bun run dev",
    "install:all": "cd frontend && bun install && cd ../backend && bun install",
    "build:frontend": "cd frontend && bun run build",
    "build:backend": "cd backend && bun run build"
  },
  "description": "BiteGo food delivery platform — monorepo root. Run frontend/ and backend/ independently.",
  "workspaces": [
    "frontend",
    "backend"
  ]
}
